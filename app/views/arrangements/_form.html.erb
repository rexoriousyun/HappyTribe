<%= form_for [timeslot, Arrangement.new] do |f| %>
  <%= f.submit value: "#{timeslot.start_time.strftime("%I-%M-%p")} - #{timeslot.end_time.strftime("%I-%M-%p")}, #{timeslot.remaining_spots} spots remaining", data: { confirm: "Are you sure you want to book this slot?" } %>
<% end %>